# Open Decisions — deferred for Kaya

Kaya's standing order (2026-07-27, before sleeping): *"Entscheidungen die ich treffen muss
entweder aufschieben oder nach eigenem Ermessen lösen."* Everything here is either **deferred**
(waiting for Kaya) or **provisionally ruled** by Apollon so the work never blocks. A provisional
ruling is reversible by one sentence from Kaya; the iteration lineage records what it cost.

| # | Decision | Status | Apollon's provisional call |
|---|---|---|---|
| K-Q1 | Is the pack + K1–K7 the binding brief? | **provisionally ruled** | Yes. The whole corpus has been built on it and nothing has contradicted it. Reversal cost: low until code exists. |
| K-Q2 | Theme authority — GM sets the campaign look, players override locally? | **provisionally ruled** | Both, layered: the GM sets the campaign skin; a player may override *locally* for accessibility (contrast, reduced motion, Clean skin) but not for content visibility. Accessibility always wins over the GM's aesthetic. |
| K-Q3 | Rule-builder priority — flagship-early or core-first? | **provisionally ruled** | Core-first. The engine contract must exist before its GUI, or the builder is a facade (the intake's own tension 2, and `03-triumph-ui-direction.md` lists "no fake rule builder before the engine contract exists" as an explicit non-goal). Schema forms may land early; the formula/action graph waits. |
| K-Q4 | Working title — keep "Project Chronicle"? | **deferred** | Keep internally. Naming is a branding decision with commercial consequences; it is Kaya's, and nothing depends on it yet. |
| K6 | Distribution — browser / Steam-native / hybrid / asymmetric | **RESOLVED** by [RB-11](../research/RB-11-steam-vs-browser-verdict.md) | Option C as architecture, ship A, Steam door unlocked and unopened. One web/DOM codebase (React/TS + PixiJS) as browser app **and** Electron client; one-time GM licence, players free, sold direct. Game-engine option struck (2/2/2). |
| — | Art production at launch — how many skin kits, first-party vs. commissioned vs. Workshop? | **RESOLVED** by RB-11 | First-party for launch; community kits through **our own registry**; Workshop only ever as a mirror. No storefront ecosystem is arriving to fund launch kits — Kaya funds them directly. |

## New decisions raised by RB-11 (ranked by downstream architectural weight)

| # | Decision | Status | Apollon's provisional call |
|---|---|---|---|
| S1 | **Do players ever pay for anything?** | **provisionally ruled** | **No — permanently, as an invariant.** Every vendor in RB-08 has retreated from all-participants-pay in one direction only. A five-person table paying €150 before a die is rolled is the adoption killer; one buyer covering a table is the shape Foundry and FG Ultimate proved. Reversal cost: high once pricing is public. |
| S2 | **Is there bandwidth for a second go-to-market motion (Steam) alongside building?** | **DEFERRED — genuinely Kaya's** | *This is the question the whole fork turns on and only Kaya can answer it.* Assumed **No** for now, which is what makes the recommendation stand. There is **no middle setting**: a Steam page nobody campaigns for is GM Forge — a dead shelf costing 30 %, 10–15 % refunds and a permanent brigadeable score. |
| S3 | One-time licence vs. recurring subscription as the primary line | **provisionally ruled** | **Licence primary**, plus optional hosted rooms priced at the cost they incur. If we ever host, follow Owlbear: charge for **storage and rooms, never for features** — Roll20 charges for features and is the most complained-about product in the market. |
| S4 | **Do we operate hosted rooms in v1?** | **DEFERRED — needs Kaya (money)** | Provisional for planning: **yes, minimal hosted rooms**, because the alternative is funding the Plex-pattern DNS + per-server certificate service in phase 0 or shipping LAN-only-with-caveats. Note honestly: *"self-hostable" on the intake page currently implies a path that does not exist.* Owlbear burns 3 TB/month at two people — this is a real bill. |
| S5 | May the Forge be a separable, separately-shippable product? | **provisionally ruled** | **Yes — as an architectural permission**, shipping decision deferred. It keeps the single highest-upside Steam play available (Dungeon Alchemist's playbook) and de-risks the venture: the tool can win even if the VTT does not. Costs nothing today beyond keeping a seam clean. |
| S6 | Business entity + public address before any sale | **DEFERRED — Kaya's, and time-sensitive** | EU DSA trader disclosure makes a solo developer's legal name and postal address **public**; a German Impressum obligation attaches to direct sales too. Also: W-8BEN + TIN must be right the first time — the Germany–US treaty takes software-royalty withholding to 0 %, and misconfiguring it costs 30 % of US revenue permanently. Needs a Steuerberater, not an agent. |
| S7 | Do we ship optional runtime AI generators at all? | **provisionally ruled** | **Yes, but strictly optional, always-draft, and off by default** — as already specified. Note the market signal: Dungeon Alchemist advertises *"does not use any generative AI/LLMs"* as a trust signal. Revisit if audience sentiment proves it a net negative; the pack always allowed the product to work without a provider. |
| S8 | Working title as the public name | **deferred, but now time-sensitive** | Under a direct-sales strategy the domain and SEO compound from day one, so this is worth settling earlier than a storefront would demand. Still Kaya's call. |
| — | Daedalus harness onboarding for this repo | **deferred** | Parked: the GPU bench is offline, and both the `local_only` lane and `decompose` need Ollama. Harvest later: structcore's semantic distillation (token efficiency) and the capability-risk taxonomy from `hierarchy.py`. |
| — | Codex's role in the loop | **provisionally ruled** | Codex works Kaya's side and writes to `design/spikes/`; the iteration arena writes to `design/iterations/`. Disjoint surfaces, no collision. Codex output enters rounds as evidence, never silently as a champion. |

## New decisions raised by `04-die-eine-plattform.md` (K8/K9 — die eine Plattform)

Raised 2026-07-27 by Pythia, synthesising RB-14..RB-18. **Revised the same day (revision 2) after
[RB-19](../research/RB-19-pruefung.md)'s audit and after reading RB-20a–d, which revision 1 had not
seen.** Full argument in [`../04-die-eine-plattform.md`](../04-die-eine-plattform.md) §9. Provisional
calls are Pythia's and are reversible by one sentence from Kaya.

> **Reconciliation with S1–S8, stated once so nothing is ruled twice.** RB-19 §3.3 found that P1 ruled
> on hosting without citing **S3** and **S4**, which already rule on it. **S3 governs and P1 now defers
> to it:** *licence primary, plus optional hosted rooms priced at the cost they incur; charge for
> storage and rooms, never for features.* **P1's revision-1 rejection of a cost-priced hosting add-on
> is overturned.** The reconciling invariant, which every hosting row below inherits:
> **no feature is ever behind a recurring charge, and no lapse in a hosting charge may ever make a
> user's own content unreadable, unexportable or uneditable.** A subscription buys features (Roll20's
> disease, and the substance of World Anvil's Trustpilot cluster and their 125 → 42 free-tier
> contraction); a metered utility buys resources. **S1** (players never pay) is untouched — but see the
> note on P4: `04` §5.2 establishes that "players always free" is **table stakes across the market**,
> so it remains an invariant and stops being a claim. **S5** (the Forge may be separable) is untouched
> and is now protected by a concrete CI rule (`packages/forge` must never import `packages/chronik`) —
> **which the M-block below ratifies as gate K-G8; `04` §8.2 orders the same gate and does not claim
> it.** **P11 is the unresolved other half of S4** and is cross-referenced there rather than re-ruled.
>
> **One scope clarification so S7 is not read as contradicting the art corpus.** **S7** rules *optional,
> always-draft, off-by-default **runtime** generators* — a GM's private draft, never canon. RB-20c §2d
> and RB-20d rule *no **generative AI in shipped art**, ever*, because AI output's protectability in
> DE/EU is contested and the exclusivity clause RB-04 requires for tier-0 art may be unobtainable — and
> because in this hobby *"no generative AI" is a marketing asset and "some generative AI" is a news
> cycle* (Dungeon Alchemist's posture; Inkarnate's October 2025 marketplace ban after a community
> revolt). **Both hold. They are different surfaces:** S7 governs a runtime draft the user asked for;
> the art rule governs bytes we ship and warrant. **Nothing AI-generated enters the bundled asset set,
> a shared template, or an export we warrant.**
>
> **And one deference, recorded rather than smoothed.** The **M1–M8** block below was raised by the
> dedicated map synthesis [`RB-20-kartenschmiede.md`](../research/RB-20-kartenschmiede.md) **after**
> `04` revision 2 was drafted. **On the map, the place layer, generation intake and the art bill, M1–M8
> govern and `04` defers** — see **P10**, which is marked superseded and names the two places where
> `04`'s argument diverges from RB-20's, so a later round can see the disagreement instead of
> inheriting an average.

| # | Decision | Status | Provisional call |
|---|---|---|---|
| P1 | **Public, SEO-indexed worlds vs. the one-time licence.** *(revised — now reconciled with S3/S4 instead of ruling separately.)* `04` §5.1 makes `die offene Tür` launch-blocking; ~€30 once does not fund a decade of unmetered public hosting with a CDN. **Note the evidence correction:** Similarweb's 47 % organic-search figure is a **channel share, desktop-only** — *"arriving at user worlds"* is an inference, not a datum, and the promotion no longer rests on it | **DEFERRED — needs Kaya (money)** | **Licence primary (S3).** Public worlds live inside a stated `die Raumuhr` allowance and **degrade honestly** when exhausted (CHAMPION §14.1). Where the allowance is not enough, an **opt-in metered room/storage utility priced at cost** — which **S3 already permits and revision 1 wrongly rejected.** The invariant above governs: features never recur; lapsing never locks a user out of her own work. The uncapped path is self-hosting, **which does not exist yet — P11.** **Reversal cost: high once pricing is public** |
| P2 | **Does the published format ship before the product?** | **provisionally ruled** | **Yes.** Versioned JSON schema for the rule package and the `.chronicle` bundle + reference parser + a non-retroactivity promise modelled on Foundry's licence clause, at launch, alongside the exporters. Days of work; the cheapest moat available; its real cost is discipline, which is why it must be published *before* the surfaces that would churn it |
| P3 | **Do we run a writing challenge (a Summer Camp / WorldEmber analogue)?** | **provisionally ruled** | **Yes, gated: not before ≥50 public `offene Tür` worlds exist.** Nearly free in engineering (a calendar, a prompt list, a badge table) and the highest growth-per-day mechanism in the competitive set — but a challenge with no participants is published evidence against us. *World Anvil does not publish its own participation figures: no reliable figure found* |
| P4 | **Is "no-code system authoring" still the wedge?** *(revised.)* Rollplay, Quest Portal, Minimal Sheets and FORGE occupy the no-code sheet-builder position that **RB-01, RB-14 §8.7 and RB-17 §5/Rank 3 each called vacant — all three are corrected by name.** Rollplay's verified clauses: drag-and-drop layout, custom-formula attributes, automated effects, dice pools, draft/shared versions, **no VTT, no maps**. *Revision 1's "a marketplace" and "freemium" did not reproduce and are withdrawn — their own line is "No subscription. No account required. Works offline."* | **provisionally ruled** | **Yes, restated — and the restatement has four clauses, because ours had one.** The wedge is authoring whose output **(1)** runs a live table, **(2)** reads a per-character knowledge projection inside its own dice math, **(3)** mints a citable result into an encyclopedia, and **(4)** exports into rivals' formats. None of the four competitors has a table. **The standalone "build your sheet without code" claim is retired from all copy** — it is now a commodity a free competitor ships. **And note against S1/S3: one-time pricing is *parity* with Rollplay, not an advantage** |
| P5 | **Which ~8 article templates, and does the curriculum ship with them?** | **provisionally ruled** | **The prompts are the feature; the templates are the container.** World Anvil's 28 templates are their cold-start answer (a curriculum, not a list) and ours is unbuilt after four rounds. Ship ~8 containers and the full question set |
| P6 | **The Erststundenbudget cap** — surfaces and named concepts from empty world to first minted paragraph | **provisionally ruled** | **≤3 distinct surfaces and ≤7 named concepts**, measured in the same instrumented session as the Prägerate gate, tuned after the first run. Paired with **die Verjüngungsregel**: no round adds a named surface without retiring one or recording why it cannot be retired. Applies retroactively to the Forge zone's four authoring surfaces |
| P7 | **Does die Schmiede (the Forge) become the lifeboat SKU if runway compresses?** | **provisionally ruled** | **Yes, with a pre-committed trigger:** maintenance:feature commit ratio above ~40 % for two consecutive months, or any calendar month with zero commits. S5 already grants the architectural permission; this attaches the trigger so the decision is not made under pressure |
| P8 | **World Anvil importer — launch or later?** *(revised to say what the rule governs.)* RB-14 §5.6 argues it is a second load-bearing importer; RB-17 Rank 4 rules one target only | **provisionally ruled** | **Later, and Rank 4 stands — as a rule about *migrations*.** Exactly one high-fidelity **migration** at launch; K10 makes it Eron/Fandom. Rank 4's whole reasoning is about *the user's irreplaceable corpus and the single moment she is willing to move*. Never ship a half-fidelity migration. **The format must still be designed so migration target two costs days, not weeks:** free today, expensive in a year. **Azgaar/FMG intake is a different question and is ruled in M7, not here** |
| P9 | **Do we ever say anything about a competitor's billing practices?** | **provisionally ruled** | **No.** Ship the model that makes the complaint impossible, state it once on the pricing page and in the lapse-behaviour spec, and never mention it again |
| P10 | **The map, the place layer and generation intake** | **SUPERSEDED — see M1–M8** | Raised in `04` §3.2(e) and §3.4 A4 before [`RB-20-kartenschmiede.md`](../research/RB-20-kartenschmiede.md) landed. **The dedicated synthesis governs and this row does not re-rule it.** `04`'s refusals agree with it (no stamp library, no paint substrate, the place layer is required anyway); **two divergences are recorded rather than averaged** — (a) on Azgaar, `04` §3.2(e) argues Rank 4 *does not reach* generated input, while **M7 argues it as an explicit exception for shape rather than population; M7's framing is the ledger's**; (b) on fusion-bearing days, `04` §4 step 8 quotes RB-20b's *"12 of Der Ortsleger's 21"*, while **RB-20 §5.4 rules that every fusion-bearing day is inside the committed half and the ≈37 marginal editor days contain none; RB-20's union arithmetic is the one to quote.** Where the two disagree, **RB-20 wins and `04` is the older document** |
| P11 | **The self-host join path — fund it, replace it, or stop claiming it?** The unresolved *other half* of **S4**. S4 asks whether we operate hosted rooms; **P11 asks what happens to the people who do not use them.** Not a duplicate: S4 is a spending decision, P11 is a capability decision | **DEFERRED — genuinely Kaya's, and it is money** | Now with measurement attached. `spike-B-wiederkehr` N1–N6 shows the ratified identity mechanism **cannot run on the default self-host topology**: `http://` LAN IP → `insecure-context`; `https://` LAN IP → `rp-id-cannot-be-an-ip`; `http://` mDNS → `insecure-context`. **Only a hosted room, the Plex-pattern DNS + certificate, and `localhost` pass.** So RB-11's *"choosing a channel does not choose a solution"* is now a measured fact, not an argument. **Provisional for planning: ratify the cookie-credential fallback** (spike-B C1–C4 — HttpOnly / Secure / SameSite=Strict, MAC-verified, revocable from *der Ausweis*) **as the self-host identity mechanism, with Athena's review and its limits printed on the page**, because it is the only branch that costs no permanent operational job. **Until one branch is chosen, no copy and no pre-committed response may route users to self-hosting** — which is why `04` §7 R-A was re-aimed. **Reversal cost: low today, high once the intake page is public** |

## New decisions raised by RB-20 (die Kartenschmiede — the integrated map tool)

Raised 2026-07-27 by Pythia, ruling on Kaya's own sentence (*„integriertes Inkarnate oder etwas
Besseres… ist aber auch gottlos schwierig vernünftig zu implementieren"*). Full argument in
[`../research/RB-20-kartenschmiede.md`](../research/RB-20-kartenschmiede.md), synthesising RB-20a–e.
Provisional calls are Pythia's and are reversible by one sentence from Kaya.

| # | Decision | Status | Provisional call |
|---|---|---|---|
| M1 | **Do you and your colleagues still have the Inkarnate/Wonderdraft *project file* for the Andaria map** — not the 8192² export, the layered source with its polylines and stamp placements? | **DEFERRED — genuinely Kaya's, and the highest-value unasked question in the corpus** | **Please ask them.** With the project file, the vector world layer is an **import, not a reconstruction**: coastlines, roads and rivers arrive as polylines, region outlines as polygons, every stamp as a placement record — turning ~9 days of authoring UI into a parser, and deciding whether the imported map can re-skin at all (our strokes above their layers vs. our strokes on a flat picture). Without it, the raster is the ceiling and everything above it is traced or generated. **One message to a colleague; it moves more money than any estimate in RB-20** |
| M2 | **Which shelf is the map on** — a feature of a wiki that plays, or a map product? | **provisionally ruled** | **A feature of a wiki. Never positioned against Inkarnate, never in a comparison table whose other column is a painting tool.** Against a map tool we lose on the only axis that category reviews on (*"6,000+ objects"* still reads as *"a very limited set"* in Dungeon Alchemist's own negative reviews). Against a worldbuilding wiki we win on every axis — and that is where the 690 red links live. Reversal cost: low today, high once marketing copy exists |
| M3 | **Is the placement editor („der Kartenleger") ever built?** | **DEFERRED — priced, gated, and Kaya's** | **Not committed, not refused.** ≈37 marginal days (≈44 with contingency) on top of the committed half, or ≈65/≈78 as a standalone union — **not the 37 or 43 the individual briefs printed.** It contains, by RB-18 §4.2's own test, **zero fusion-bearing days**: every one of them is already inside the committed half, so deferring costs no differentiator at all. **Ask again only when `S-P1`, `S-G1` and `S-K1` are green and someone has asked twice.** He is entitled to spend it; he is not entitled to spend it believing it is 37 |
| M4 | **When is art money spent, and how much before revenue?** | **provisionally ruled** | **€0 until a paying user exists.** Then €1,100–2,200 for the twelve assets that photograph one biome; €2,700–8,200 for a full 60-asset style, only once somebody has asked twice. The art is the **only calibrated line item in this project** (published per-asset rates) and also **the only one payable in cash by a builder with none** — those two facts are the same fact. RB-20c's *"the art is not the risk"* is adopted for its method and rejected for its conclusion |
| M5 | **K1's map-reskin claim — narrow it now?** | **provisionally ruled** | **Yes: „die Häutungsregel" — the map re-skins where we drew it and does not re-skin where they drew it.** Fills, coastlines, roads, rivers, labels, markers and chrome wear the theme; the imported raster and any commissioned stamp set stay one style. Gate **K-G6** falsifies it with one Aphrodite mockup and thereby saves a **€14,400–65,800** four-theme commission from being scheduled by assumption |
| M6 | **Azgaar's FMG — vendor, fork, or consume the export?** | **provisionally ruled** | **Consume the export. Never fork.** 18,165 lines of MIT-licensed generators (plus an explicit commercial-derivative-works clause), pushed the day before the brief — but **698 references to a global `pack`, 97 to `grid`, 19 of 40 files importing d3, 11 DOM call sites.** Porting is a permanent maintenance fork; running it unmodified and writing a parser is days. Ledger it as `LicenseRef-Azgaar-FMG-1.0` with a text snapshot — GitHub classifies it `NOASSERTION`, not MIT, and the CI licence gate will otherwise mis-fire |
| M7 | **A second launch import (Azgaar GeoJSON/JSON) against RB-17 Rank 4's "exactly one"** | **provisionally ruled** | **Yes, and argued explicitly rather than smuggled past the rule.** Rank 4's *one* high-fidelity import is chosen for **population** and K10 makes it Eron/Fandom. This is a second import chosen for **shape**: it is the only structured world-data interchange the category has, it is MIT, and it costs days. **The world-map interchange target is not UVTT** — the two tools that made Kaya's own map emit no wall data and never will |
| M8 | **Does „die Schmiede" (the Steam-shaped separable half) still mean a map editor?** | **provisionally ruled** | **No — it means the generator, the theme editor and the visual rule-builder.** The map editor is not inside the Forge's definition until M3 rules yes. **The 3-day package boundary is taken regardless** (`packages/forge` may never import `packages/chronik`, enforced in CI, gate K-G8): cheap now, impossible to repair later, and RB-11's separability ruling — the Dungeon Alchemist pattern, the only €2.46 M in this market — dies quietly without it |

## New decisions raised by RB-21 (die Weltmaschine — Verschachtelung, Inventar, Erzeugung)

Raised 2026-07-27 by Pythia, ruling on Kaya's own sentence (*„jedes Universum ist dann eine gigantisch
große nested map. Mit nested inventory"*), synthesising RB-21a–e. Full argument in
[`../research/RB-21-weltmaschine.md`](../research/RB-21-weltmaschine.md). Provisional calls are
Pythia's and are reversible by one sentence from Kaya.

> **What was ruled and is therefore *not* here.** The containment contract itself is decided, not
> deferred, because it is the most irreversible object in the product and Kaya's own migration rule
> governs it: `Raum` is a strict tree, `Bezug` is a typed dated multigraph, `Ort`/`Ding`/`Posten` is
> one lattice with two one-way human promotions, an actor is not a node, `MAX_TIEFE = 24`, and
> R-P1…R-P4 are the only visibility implications that exist. Those are columns and constraints, not
> spending. **Everything below is either money, a public claim, or a question no measurement in the
> corpus can answer.**
>
> **And one deference, recorded rather than smoothed:** on the map, the place layer, generation intake
> and the art bill, **M1–M8 govern and RB-21 defers.** Where RB-21 §4 rules on generation it rules on
> *shape and containment* — which file we ingest and what a child node is — never on whether the map
> is built. **M3 and M7 are untouched and are not re-opened.**

| # | Decision | Status | Provisional call |
|---|---|---|---|
| N1 | **Is inventory a knowledge surface or a physics surface?** RB-21a §5.3 L5 raises it explicitly and refuses to settle it: a player's carried weight is computed over *her own projection* (`getragen`), while ground truth (`wahr`) is GM-only — so a bag can be heavier than it looks and the number never says so until the GM releases a passage. **A table that wants strict physical realism will experience this as a bug** | **provisionally ruled** | **Knowledge surface, because that is what the champion is.** The GM gets one gesture (*„schwerer als er aussieht"*) that releases a passage — which is a mint, which is the existing mechanism, which is the whole product's shape. Reversal cost: **low today (nothing is built), high once `Fassung` ships**, because the alternative is a client that sums and therefore leaks (gate W-G5). If Kaya's own table wants physics, say so before N2 is answered |
| N2 | **Does the item half get built at all — 28→34 developer-days on a ledger already at ≈289–301?** RB-21b's own split: **≈13 of 28 days buy something no rival can be bought to do (46 %, the second-best fusion ratio in the lineage); the other 15 buy parity with what Foundry's `dnd5e` has shipped since 2024-01-31** | **DEFERRED — genuinely Kaya's, and it is money** | **Not committed, not refused — the same shape as M3.** RB-21 cuts all 28 days from this round and returns them with the real number attached. **Ask again only when `S-P1` (2 d) and `S-R1 · Die Hülle` (1 d) are green**, because RB-21b's own note says its 5-day `Sicht` line *is not bounded until `S-P1` runs* and building on an unbounded line is how a 5 becomes a 20. **If it is ever built, RB-18 route item 5 binds: the 13 fusion-bearing days first, the 15 parity days last.** He is entitled to spend it; he is not entitled to spend it believing this round was free |
| N3 | **Do we embed Azgaar's generator (a stripped fork, driven headlessly) or stay import-only?** Embedding is the only thing that turns `seed → world` into a function whose calling convention we own — real value, and it is what makes a pinned option vector honest | **provisionally ruled — import-only, and the embed is conditioned** | **Import-only for slice 1 and 2.** RB-21d's Shape B1 as written — *"we ship their `dist/`"* — is **refused**: `publicDir: '../public'` means `npm run build` copies all 21.49 MB of `public/` verbatim, so shipping `dist/` ships **both of Athena's CRITICALs** (179 CC-BY-NC-SA charges; TinyMCE 7.1.0 GPLv2+) **plus a 2016 jQuery with three published CVEs**. Import-only owes none of it. **The embed unlocks when: the strip is priced (nobody has), gates „Saubere Tüte", „Vollständige Nennung" and „Der alte Kasten" are green, and Athena re-reviews the stripped tree.** Reversal cost: low — `ErzeugerAdapter: (Weltkeim) → Ort[]` makes import, embed and reimplement three implementations of one interface |
| N4 | **Does a generated or imported world's machine-written prose ever reach the reader, and in which language?** One FMG run emits **5,915 characters of English legend text** and 664 names in invented phonologies — into a German world. K7 territory | **provisionally ruled** | **Import the structure, suppress the prose, opt-in only.** What is imported lands as `kind: rohblock` inside a bordered card headed *„Vom Generator erzeugt — nicht geprüft"* with generator, version and `keim_hash` beneath: **citable, addressable, never a `Quelle`.** Identical to RB-12 §2.8's treatment of an unconvertible wikitable, for the identical reason — *a silently dropped construct is a lie about completeness, and a silently promoted one is a lie about authorship*. **No reliable figure found** for whether FMG localises `notes` at all; it does not appear to |
| N5 | **Does the public claim about nested maps change?** **Nested maps are parity, not novelty.** LegendKeeper's own front page: *„Nest your maps indefinitely, zoom from continents to crypts, and link every landmark to its wiki page"* — with a published tutorial, so the feature ships | **provisionally ruled** | **Yes, and it narrows to the sentence after theirs.** Never claim nesting. Claim the **projection down** the nesting: their permission wording is *„invite them as free collaborators or send them a public viewing link. They see what you want, nothing more"* — share-level, per-viewer at best, **no `erfahrungsgrad`, no fog, no per-character predicate.** *Die Verschachtelung ist der Eintrittspreis; die Projektion ist die Fusion.* And a second copy rule from the same ruling: **„die Verschachtelung ordnet ungefähr jede fünfte Tür — und das ist eine Untergrenze"** (measured two independent ways today), never *„a large share"* and never *„containment defuses the doors problem."* Reversal cost: low today, high once marketing copy exists |
| N6 | **Is the GM ever asked „wo hängt das?" before she can play?** RB-21a makes an unparented node structurally unrepresentable; RB-21b makes it legal. At 21:04 on a Saturday that difference is the whole product | **provisionally ruled** | **Never on first placement.** A per-campaign **`Unverortet`** root — one row, one name, one breadcrumb reading *„noch nicht verortet"*. It satisfies the one-parent invariant, it lets her play tonight, and it turns the graph question into a *later* question, which is the only kind a tired GM can afford. Re-parenting later is one keypress and an audit row. **Gate W-G9 makes the claim falsifiable:** the containment graph must contribute **zero** of P6's ≤7 named concepts on the path to a first minted paragraph |
| N7 | **Does `Nachrechnen` extend over a generated world?** `Weltkeim` promises *"everything derived from it is re-executable, provably"* — but re-deriving requires still being able to **execute that exact generator version**, and id survival across an FMG version bump is untested | **provisionally ruled — the claim is narrowed, not defended** | **No. `Weltkeim` is a provenance record, not a re-derivation promise, and `Nachrechnen` is not claimed over a generated world in any copy or any UI until id survival across a version bump is tested.** Import-only (N3) makes this comfortable rather than painful: the `.map` file the GM dropped **is** the artifact and we keep it. The struct is stored anyway — ~100 bytes, and a world stored without its complete option vector can never be re-derived at all (**0 of 664 burgs keep `(id, name)`** across a canvas change on the same seed). Reversal cost: none — narrowing a claim is free, widening it later is a press release |
| N8 | **Foundry shipped „Scene Levels" into core in v14. CHAMPION §9.5 has refused it for years — is that refusal re-read?** | **DEFERRED — Apollon's, at the next champion revision** | **Not re-ruled here, and flagged so it cannot be missed:** the refusal is now a refusal of a **core competitor feature**, not of a third-party module. **The asymmetry is in our favour and it is free:** this ruling's schema already carries elevation as a scalar band on the node (`hoehe_von` / `hoehe_bis` / `ordinal`, with the underdark as a *sibling* at a negative band, per IMDF's `ordinal` and OSM's `level=-1`), so **we may hold the *data* of scene levels without building the renderer.** Foundry's own release note calls the feature *"still in active development and it will continue to evolve significantly"* [Herstellerangabe] |
| — | **Does any GM want nested containment?** Not a decision — the round's largest unmeasured assumption, logged here because it now carries N2's 28→34 days | **open, and cheap to close** | **Zero demand measurements exist in either direction, in this round or any prior one.** The market evidence is contradictory and both halves are real: **Roll20 has fifteen years of continuous, specific requests** (nested containers, collapsible containers, weight-calculating repositories, packs-with-contents) **and still does not ship it**, for a structural reason rather than a priority one; **Foundry's `dnd5e` took three years three months to ship it and broke permissions within twenty-four hours.** Neither settles it. **Asking two GMs costs one message and should happen before N2 is answered** |

## Rulings Apollon made without asking (logged for review)

- **Renumbered** `design/02-triumph-ui-direction.md` → `03-` because it collided with
  `02-domain-model.md`. Codex's internal citation of `02-domain-model.md` still resolves.
- **Adopted** the Triumph UI direction as binding for the iteration rounds (K7), with the
  reservation that its 10-point acceptance matrix is **self-graded** and gets independently
  attacked inside the rounds.
- **Did not re-run** the K5 map/rendering research — Codex's RB-02/04/05 cover it.
- **Deferred the Timeline table** from the domain model to a nullable column (Mēden agan).
